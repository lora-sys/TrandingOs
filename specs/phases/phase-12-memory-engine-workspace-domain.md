# Phase 12 - Memory Engine Workspace Domain

## Goal
Upgrade Memory and Workspace into long-term OS domains.

## Scope
Conversation, research, trade, review, skill, strategy, workspace memory plus workspace links.

## Tasks
- [x] Expand memory schema with domain, workspace, source, importance, metadata.
- [x] Add memory query/write APIs.
- [x] Automatically write workspace, research, trade plan, journal, review, and strategy memory.
- [x] Add workspace context/memory/artifacts APIs.
- [x] Show workspace memory and linked artifacts in Workspace UI.

## Deliverables
Memory Engine contract, Workspace Domain APIs, UI tables, automatic memory writes.

## Acceptance Criteria
Workspace records can bind memory and artifacts; review/evolution can read memory context.

## Test Plan
Run unit/API smoke and Playwright workspace -> research -> memory visibility path.

## Demo Requirement
Save `output/playwright/phase-12-memory-workspace.png` and video when available.
